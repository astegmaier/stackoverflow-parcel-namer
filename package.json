{
  "private": "true",
  "workspaces": [
    "parcel-namer-js-min",
    "app"
  ],
  "scripts": {
    "clean": "yarn workspaces run clean",
    "build": "yarn namer build && yarn app build",
    "format": "yarn namer format && yarn app format",
    "namer": "yarn workspace parcel-namer-js-min",
    "app": "yarn workspace app"
  }
}
